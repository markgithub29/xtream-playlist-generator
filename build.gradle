// build.gradle (root)
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.0.2" // Match the required version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

